{
  "name": "vaadi-foods-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --prefix apps/store",
    "dev:admin": "npm run dev --prefix apps/admin",
    "build": "npm run build --prefix apps/store && npm run build --prefix apps/admin",
    "lint": "npm run lint --prefix apps/store && npm run lint --prefix apps/admin",
    "start": "npm run start --prefix apps/store"
  },
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
